#include <stdio.h>
int main() {
    int x = 4, y = 8;
    if (x < y && x > 2) {
        if (y < 10) {
            printf("x es menor que y y mayor que 2, y y es menor que 10.\n");
        }
    }
    return 0;
}
